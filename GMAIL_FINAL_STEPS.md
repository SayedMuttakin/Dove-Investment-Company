# 📧 Gmail দিয়ে Email Setup (সহজ বাংলা গাইড)

## আপনার App Password: `zqeg bznt aydq lvjp`

---

## ✅ Step 1: VPS এ Setup করুন (খুবই সহজ)

আপনার VPS এ লগইন করে নিচের কমান্ডগুলো দিন:

### 1. .env ফাইল ওপেন করুন:
```bash
cd /var/www/novaearn/server
nano .env
```

### 2. ইমেইল কনফিগারেশন আপডেট করুন:
ফাইলে নিচে স্ক্রল করে **Email Configuration** সেকশনে আপনার তথ্যগুলো এভাবে বসান:

```env
# Email Configuration (Gmail)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=আপনার-জিমেইল-এখানে-লিখুন@gmail.com
SMTP_PASSWORD=zqeg bznt aydq lvjp
SMTP_FROM_EMAIL=আপনার-জিমেইল-এখানে-লিখুন@gmail.com
SMTP_FROM_NAME=NovaEarn
```

> **টিপস:** `আপনার-জিমেইল-এখানে-লিখুন@gmail.com` এর জায়গায় আপনার নিজের Gmail অ্যাড্রেসটি লিখে দিন।

**সেভ করার নিয়ম:**
- কিবোর্ডে `Ctrl + O` চাপুন, তারপর `Enter` চাপুন।
- এরপর `Ctrl + X` চেপে বের হয়ে আসুন।

---

## ✅ Step 2: প্রজেক্ট রিস্টার্ট করুন

এখন নিচের এই কমান্ডগুলো এক এক করে দিন:

```bash
# ১. গিট থেকে লেটেস্ট কোড নিন (যদি না নিয়ে থাকেন)
cd /var/www/novaearn
git pull origin master

# ২. সার্ভার ফোল্ডারে গিয়ে লাইব্রেরি ইন্সটল করুন
cd server
npm install

# ৩. প্রজেক্ট রিস্টার্ট দিন
pm2 restart all
```

---

## ✅ Step 3: টেস্ট করুন

১. আপনার ওয়েবসাইট https://doveinvestment.cloud এ যান।
২. একটি টেস্ট উইথড্র বা ডিপোজিট করুন।
৩. আপনার ইনবক্স চেক করুন ইমেইল আসছে কি না।

যদি কোনো সমস্যা হয়, VPS এ `pm2 logs` লিখে আমাকে এররটি জানান।
